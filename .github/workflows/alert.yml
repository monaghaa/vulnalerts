name: VulnAlerts

on: 
  schedule:
    - cron:  '0 * * * *'

jobs:
  alert:
    runs-on: rocky
    steps:
    - uses: actions/checkout@master
    - uses: monaghaa/vulnalerts@master
      env:
        SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
    - name: done
      run: echo 'done'
